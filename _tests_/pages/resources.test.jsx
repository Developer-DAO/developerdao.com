import React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import testCommonLink from '../utils/testCommons';

import ResourcePage from '../../src/pages/resources';

// Mocks
// - may be worth mocking Airtable resources to prevent relying upon an external resource to test
// - particularly to return errors on demand

// Tests

// Resource error

// Author error

// Successful load
